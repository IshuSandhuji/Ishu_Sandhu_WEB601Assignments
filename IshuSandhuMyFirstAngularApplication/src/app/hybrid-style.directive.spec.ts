import { HybridStyleDirective } from './hybrid-style.directive';

describe('HybridStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new HybridStyleDirective();
    expect(directive).toBeTruthy();
  });
});
