import { NoTypeStyleDirective } from './no-type-style.directive';

describe('NoTypeStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new NoTypeStyleDirective();
    expect(directive).toBeTruthy();
  });
});
