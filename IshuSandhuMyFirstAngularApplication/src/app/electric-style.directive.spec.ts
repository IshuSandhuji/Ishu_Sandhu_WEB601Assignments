import { ElectricStyleDirective } from './electric-style.directive';

describe('ElectricStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new ElectricStyleDirective();
    expect(directive).toBeTruthy();
  });
});
